
const axios = require('axios')
const { response } = require('express')

require('dotenv').config() // nos permite acceder a la variables de .dotenv
// me habilita tener el objeto procces. env que tiene las variables.
const URL=process.env.API_URL

const getAdd = (req, res) => {

}

module.exports = getAdd